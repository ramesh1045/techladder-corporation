# Server
PORT=3000
NODE_ENV=development

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Ramesh@1045
DB_NAME=techladder_corporation
DB_CONNECTION_LIMIT=10

# JWT
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=8h

# Initial admin (used ONLY by `npm run seed:admin`, never read at runtime)
ADMIN_EMAIL=mediaagencytlc@gmail.com
ADMIN_PASSWORD=Techl@dder123

# CORS - comma separated list of allowed frontend origins
CORS_ORIGIN=http://localhost:4200

# Uploads
MAX_VIDEO_SIZE_MB=200
MAX_THUMBNAIL_SIZE_MB=5

# Public base URL backend runs on (used to build absolute media URLs)
PUBLIC_BASE_URL=http://localhost:3000
