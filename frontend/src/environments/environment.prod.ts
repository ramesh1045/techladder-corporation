export const environment = {
  production: true,
  // Replaced at build time — set these to your deployed backend's real URL.
  // NEVER leave these pointing at localhost in a production build.
  apiBaseUrl: 'https://api.techladdercorporation.com/api',
  mediaBaseUrl: 'https://api.techladdercorporation.com'
};
